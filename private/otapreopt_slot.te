typeattribute otapreopt_slot coredomain;
typeattribute otapreopt_slot nonprofilable;

# Technically not a daemon but we do want the transition from init domain to
# cppreopts to occur.
init_daemon_domain(otapreopt_slot)
