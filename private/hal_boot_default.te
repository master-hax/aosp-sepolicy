# boot_control subsystem
type hal_boot_default, domain, boot_control_hal;
hal_impl_domain(hal_boot_default, hal_boot)

# may be started by init
type hal_boot_default_exec, exec_type, file_type;
init_daemon_domain(hal_boot_default)
