# HwBinder IPC from client to server, and callbacks
binder_call(hal_iris_client, hal_iris_server)
binder_call(hal_iris_server, hal_iris_client)

add_hwservice(hal_iris_server, hal_iris_hwservice)
allow hal_iris_client hal_iris_hwservice:hwservice_manager find;

# For memory allocation
allow hal_iris ion_device:chr_file r_file_perms;

r_dir_file(hal_iris, cgroup)
r_dir_file(hal_iris, sysfs)
