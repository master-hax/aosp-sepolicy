# contexthub subsystem
type hal_contexthub, domain;
type hal_contexthub_exec, exec_type, file_type;

# hwbinder access
hwbinder_use(hal_contexthub)

# call into system_server process (callbacks)
binder_call(hal_contexthub, system_server)
